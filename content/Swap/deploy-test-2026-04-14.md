# Deploy Test Note

This file exists to trigger the incremental GitHub Pages deployment workflow for the `Swap` subtree.

- Date: 2026-04-14
- Purpose: verify path-filtered subtree build + `gh-pages` overlay deploy
